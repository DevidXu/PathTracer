#pragma once

// This file defines the operation of a ray and about how to generate the new ray when 
// it hits some material.

class Ray {
private:

public:

};

typedef std::vector<shared_ptr<Ray>> PixelRays;